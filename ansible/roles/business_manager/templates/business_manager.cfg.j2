CompName={{CompName}}
CompFlag={{CompFlag}}
LocalAddr={{LocalAddr}}

#日志配置
LogMask={{LogMask}}
#单个日志文件最大限制，单位M
MaxLogFileSize={{MaxLogFileSize}}
LogQueueMask={{LogQueueMask}}

#控制监听端口
ListenPort={{ListenPort}}

#证书配置，证书路径结尾必须带/
SSLPath={{SSLPath}}
SSLCaFile={{SSLCaFile}}
SSLKeyFile={{SSLKeyFile}}
SSLPasswd={{SSLPasswd}}

#总线配置
MQServer={{MQServer}}
MQPort={{MQPort}}

#漏扫任务超时时间 30*24*60*60
VUL_TASK_TIMEOUT={{VUL_TASK_TIMEOUT}}

#数据库配置
DBUser={{DBUser}}
DBPassword={{DBPassword}}
DBHost={{DBHost}}
DBPort={{DBPort}}
DBName={{DBName}}

#监控框架类型,0:monitor 1:urm
MonitorType={{MonitorType}}
